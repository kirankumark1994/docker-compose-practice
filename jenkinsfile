pipeline {
    agent any

    stages {
        stage('checkout') {
            steps {
               git 'https://github.com/kirankumark1994/docker-compose-practice.git'
            }
        }
       
        stage('up') {
            steps {
                sh docker-compose -f docker-compose.yaml up -d
            }
        }
        stage('ps') {
            steps {
                sh docker -f docker-compose.yaml ps
            }
        }
    }
    post {
  success {
    echo "success"
  }
  failure {
    echo "failure"
  }
}

}
